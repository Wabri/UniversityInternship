(function () {
    'use strict';

		angular.module('revux')
		  .filter('join', function() {
		    return function(array, separator) {
		      if (!array) {
		        return '';
		      }
		      return array.join(separator);
		    };
		  }
		);

}());