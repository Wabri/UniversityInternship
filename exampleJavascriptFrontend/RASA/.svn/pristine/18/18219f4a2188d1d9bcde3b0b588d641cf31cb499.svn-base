(function() {
  'use strict';

  angular
    .module(APP_NAME)
    .controller('LayoutCtrl', LayoutCtrl);

  /**
   * [MainCtrl description]
   * @method      MainCtrl
   * @param       {[type]} $scope     [description]
   * @param       {[type]} $state     [description]
   * @param       {[type]} $rootScope [description]
   * @constructor
   */
  function LayoutCtrl($scope, $state, $rootScope, bvPromisesFactory) {

    var vm = this;

    // LE FUNZIONI esposte
    vm.test = test;

    // attesa direttiva
    vm.waitApi = {};
    vm.waitPromise = {};

    // parte da qui
    init();

    /**
     * init della procedura
     * @method init
     * @return {[type]} [description]
     */
    function init() {

      // inizializzazione
      //var waitDirectives = bvPromisesFactory.instance();

      // promessa vera per attesa DIRETTIVA MOVIMENTI
      //vm.waitPromise = waitDirectives.setPromise();

      // le chiamate per attesa
      //waitDirectives.wait(function() {
      // attesa didattica
      //});
    }

    // dichirazione esposte
    function test() {
      console.log("controller LayoutCtrl funnzione test");
    }

  }

}());