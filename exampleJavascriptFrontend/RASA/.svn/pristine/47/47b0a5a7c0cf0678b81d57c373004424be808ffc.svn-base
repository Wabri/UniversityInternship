/**
 * homepageCtrl : controller home page
 * @type {[type]}
 */
angular.module(APP_NAME)
  .controller('interviewCtrl', interviewCtrl);

/**
 * [interviewCtrl description]
 * @method interviewCtrl
 * @param  {[type]}     EVENTS [description]
 * @return {[type]}            [description]
 */
function interviewCtrl($scope, $rootScope, $q, $timeout, $http, $mdDialog, $translate, bvApiService, bvAlertService, bvBusyService, EVENTS) {

  // bp
  var vm = this;
  vm.project = {

  };

  vm.data = {};

  vm.checked2 = 'vittima';
  vm.checked = 'no';
  vm.checked3 = 'persone del posto';








}
